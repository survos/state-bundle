{#
  DEPRECATED — do not include this partial.

  The marking + enabled-transitions UI (with synchronous Apply) is now the
  self-contained Twig/UX component:

      <twig:state:workflow-marking
          :subject="entity"
          workflowCode="<flow name>"
          globalKey="<APP_ENTITY_* key>"
          layout="table" />

  The component reads the marking, lists enabled/blocked transitions, and runs
  a transition synchronously via the built-in survos_state_debug_apply route —
  no app-specific transition_path controller required. The subject must
  implement RouteParametersInterface (field-bundle) so erp.entityId resolves.
#}
<div class="alert alert-danger" role="alert"
     style="border:3px solid #d63939; font-size:1.05rem;">
  <strong style="font-size:1.25rem; display:block; margin-bottom:.25rem;">
    ⚠ DEPRECATED: <code>@SurvosState/_entity_transitions.html.twig</code>
  </strong>
  This include has been removed. Use the
  <code>&lt;twig:state:workflow-marking&gt;</code> component instead — it renders
  the marking and runs transitions synchronously on its own. See the comment at
  the top of this template for usage.
</div>
